class Direction {
  int distanceValue;
  String distanceText;
  int durationValue;
  String durationText;
  String encodedPoints;

  Direction(
      {this.distanceValue,
      this.distanceText,
      this.durationValue,
      this.durationText,
      this.encodedPoints});
}
